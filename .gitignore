.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Secrets live here only, never committed (same rule as the pipeline).
.env

# Runtime state: the message bus and any run artifacts.
data/
*.db
*.db-shm
*.db-wal
